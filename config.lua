config = {}

config.toggleKey = "Grave"  -- https://docs.fivem.net/docs/game-references/input-mapper-parameter-ids/keyboard/
                            -- May not work with all keys. You can have a look at client/functions.lua/getJSKey() for the lookup table and add your own if needed

config.keybindMessage = "UI: Toggle Controls"   -- This is the message that will be displayed in the control settings